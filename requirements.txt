# Prism runtime dependencies.
#
# There is deliberately no deep-learning framework here. The convolutional
# network in prism/convnet.py is written in NumPy, which keeps the deployed
# image small enough to install reliably on Streamlit Community Cloud.

streamlit>=1.50,<2
pandas>=2.0,<4
numpy>=1.26,<3
scikit-learn>=1.4,<2
scipy>=1.11,<2
plotly>=5.22,<8
